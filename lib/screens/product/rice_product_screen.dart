import 'package:flutter/material.dart';

class RiceProductScreen extends StatelessWidget {
  const RiceProductScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text("Beras")));
  }
}
