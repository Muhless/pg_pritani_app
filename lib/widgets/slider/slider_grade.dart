import 'package:flutter/material.dart';

class SliderGrade extends StatelessWidget {
  const SliderGrade({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.lightGreen, child: Text('Grade A'));
  }
}
